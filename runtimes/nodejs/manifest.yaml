project:
  namespace: _
  packages:
    $PACKAGE_NAME:
      inputs:
        bucket: $BUCKET
      actions:
        app:
          function: actions/app.js
          runtime: nodejs:8
          web-export: true

