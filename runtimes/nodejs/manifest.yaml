project:
  namespace: _
  packages:
    $PACKAGE_NAME:
      inputs:
        bucket: $BUCKET
      actions:
        initial-html:
          function: actions/initialHtml.js
          runtime: nodejs:8
          web-export: true

